import config from "../../../config";

const Media = {
  profileImage: `${config.BASE_URL}/upload/media/profile_image`,
  treatment_Image: `${config.BASE_URL}/upload/media/provider_portfollio`,
};

export default Media;
